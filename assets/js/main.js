// Tiện ích chung cho toàn site
document.addEventListener('DOMContentLoaded', () => {
  // Không cần logic đặc biệt ở đây — mỗi module (KMH/grammar/extra/blog)
  // có script riêng. File này để dành cho các tiện ích chung sau này
  // (ví dụ: đánh dấu link nav đang active tự động nếu cần).
});
